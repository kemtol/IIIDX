# 2026-05-10 11:13 WIB — IPOT TREND_1D fallback for yfinance_1h

Scope: inference-side operational fallback for the 15:05 WIB race condition.

Changes:
- Added `pipeline/fetch/fallback_ipot_trend_to_yfinance_1h.py`.
- The script checks missing `yfinance_1h` rows for a target WIB candle, aggregates `ws_trend_1m.duckdb` `TREND_1D` rows into compatible 1h OHLCV, and writes only missing `(datetime, ticker)` keys.
- Existing Yahoo rows are preserved; fallback rows never overwrite them.
- Added dry-run/upsert modes and JSON audit logs under `_LOG/`.
- Added unit tests in `pipeline/fetch/tests/test_fallback_ipot_trend_to_yfinance_1h.py`.

Verification:
- `python3 -m py_compile pipeline/fetch/fallback_ipot_trend_to_yfinance_1h.py`
- `/home/kemal/.venv/bin/python -m pytest pipeline/fetch/tests/test_fallback_ipot_trend_to_yfinance_1h.py -v`
- Dry-run for `2026-05-08` hour `14` returned `PASS_FALLBACK_PARTIAL`: yfinance had 0/773 target rows; IPOT trend could fill 159 rows.

Operational command:

```bash
python3 pipeline/fetch/fallback_ipot_trend_to_yfinance_1h.py \
  --date YYYY-MM-DD \
  --hour 14 \
  --mode upsert
```

Notes:
- Current `trending_all` table has no `date` or `captured_at`, so the script assumes all rows belong to the target date and emits a warning. This should be improved before production cron by adding an explicit date/capture column to the recorder sync.
