# 2026-05-04 — Go Inference Parity: Overnight + Stockbit + Preclose14 + Broker Audit + Cron

- **Date:** 2026-05-04 22:36 WIB
- **Scope:** Close Go inference parity gaps. Rewrite broken features, calibrate, fix cron, update docs.
- **Product:** Inference (Go)
- **Predecessors:** `20260504163209.md` (Stockbit/XL + v20 policy)

## Completed

### Rewrites & Fixes
1. **Overnight** (`overnight.go`) — full rewrite: 1h bars → daily parquet. Was computing from sparse hour-9/15 extraction (gaps); now reads complete daily OHLCV. 24/27 cols PERFECT calibration; 3 cols p10 quantile epsilon.
2. **Stockbit/XL** (`stockbit.go`) — NEW: DuckDB SQL from broksum L0. 4 cols: xl_buy_freq_ma5, xl_sell_freq_ma5, xl_buy_freq_ma20, xl_freq_surge. 98/98 PERFECT.
3. **Preclose14 volume** (`preclose14.go`) — FIX: rollingMAShifted min_periods was `max(3, window/2)` (Go), should be `max(3, window/4)` (Python). Daily vol/turnover MA 6/6 PERFECT after fix. 2 sparse tickers (BLTZ, BSWD) with <20 days lookback have diff.
4. **v20 ARA policy** (`main.go`) — NEW: veto filter in predict path. Auto-activates for v19d/v20 variants. Keeps single_release + near_ara_not_touched_0_3pct.
5. **YF download** (`fetcher/yahoo.go`) — NEW: `bsjp download` command. Reads yf1h parquet for ticker list, calls Yahoo Finance v8 API, appends new bars. ~0.2s/ticker, dedup by (date,ticker).
6. **Cron** (`run_inference_bsjp.sh`) — REWRITE: Python path gone. Now: `bsjp fetch` → `bsjp predict v19d` → `bsjp predict v15`. Auto-builds binary.

### Calibration Verdicts
| Module | Cols | Result | Root Cause |
|--------|------|--------|------------|
| Global | 15 | PERFECT | — |
| Momentum | 4 | PERFECT | — |
| yf_daily | 58 | PERFECT | `.JK` suffix fix |
| Overnight | 27 | 24/27 PERFECT | 1h→daily rewrite |
| Stockbit | 4 | PERFECT | New implementation |
| Preclose14 vol | 6 MA | PERFECT | min_periods fix |
| Broker base flow | 18 | PERFECT | Already correct; stale module was wrong benchmark |

### Key Finding: Broker Calibration Was a False Alarm
- Compared Go vs Python MODULE (stale snapshot) → saw "101 diffs"
- Compared Go vs fresh Python from SAME CURRENT L0 → 18/18 PERFECT
- DuckDB `INSERT OR REPLACE` preserves unlisted columns (verified) — broker values from bootstrap survive daily fetch
- 26 flow cols refreshed daily (correct), 96 timeflow/context cols from bootstrap (stale but correct for training dates)

## Architecture Decisions
- **No L1 dependency** — all Go features compute directly from L0 parquet
- **L0 cron dependency** — 4 L0 files from Python fetchers: yfinance_daily, yfinance_1h, broksum, global_indices
- **`bsjp download`** redundant for production (Python cron handles yf 1h); useful for manual intraday
- **Broker Phase 2-3 deferred** — 96 cols needed only when inference passes bootstrap date range

## Files Changed
- `inferences/bsjp/golang/internal/features/overnight.go` — REWRITE (daily data)
- `inferences/bsjp/golang/internal/features/stockbit.go` — NEW
- `inferences/bsjp/golang/internal/features/preclose14.go` — FIX (rollingMAShifted)
- `inferences/bsjp/golang/internal/fetcher/yahoo.go` — NEW
- `inferences/bsjp/golang/cmd/bsjp/main.go` — download cmd, v20 policy, overnight wiring, stockbit wiring
- `pipeline/run/run_inference_bsjp.sh` — REWRITE (Go binary)
- `AGENTS.md`, `program.md` — updated

## Next Priorities
1. Version-lock L0 for next training
2. Paper-trade v20 policy
3. Broker Phase 2-3 (96 cols)
4. Cross-sectional audit (sq_/xc_/yp_/pd_)
