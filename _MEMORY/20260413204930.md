# Session Memory (Compact)

- Date: 2026-04-13 (Asia/Jakarta)
- Scope: Broksum datamart/training pipeline alignment, path cleanup with backward-compat, label objective refinement (open 09:00 -> close 10:00), and localfund-focused feature expansion.

## Key Decisions

1. Keep parquet-based pipeline for current ML workflow (daily append/refresh still acceptable); postpone DB migration.
2. Maintain compatibility while cleaning structure:
   - Canonical training script moved to `machinelearning/service/training/`
   - Legacy `Level 2` paths kept alive via wrapper + mirror outputs.
3. Use screener objective based on morning move:
   - Entry = open 09:00
   - Exit reference = close 10:00
   - Label TP = close@10:00 >= open@09:00 * 1.03
4. Shift strategy emphasis from single broker anchor (MG) toward stronger localfund confluence signals.

## Major Changes Implemented

### Pipeline & Path Cleanup
- Canonicalized training generator:
  - `machinelearning/service/training/generate_training_datamart_opening.py`
- Canonical runner:
  - `machinelearning/service/training/script/do_generate_training.sh`
- Added backward-compat wrappers:
  - `machinelearning/service/Level 2/generate_training_datamart_opening.py`
  - `machinelearning/service/Level 2/script/do_generate_training.sh`
- Output policy:
  - Primary write: `machinelearning/data/training/`
  - Mirror write enabled by default to legacy `machinelearning/data/Level 2/`

### Label Definition Update
- Reworked label builder in training generator:
  - Uses 1H candle timestamp `10:00` as interval proxy for `09:00 -> 10:00`
  - `entry_price_opening` = candle `open` (09:00)
  - `close_to_cutoff` = candle `close` (10:00)
  - `label_tp` now computed from `close_to_cutoff`, not `high_to_cutoff`
- Validation:
  - `entry_hm` now consistently `900`
  - Rule check matched (`label_close_rule_match = 1.0`)

### Localfund Feature Expansion
- Added stronger localfund features into training aggregate:
  - participation/intensity:
    - `localfund_participation_ratio`
    - `localfund_buyer_ratio`
    - `localfund_consensus_strength`
  - concentration:
    - `localfund_concentration_hhi`
    - `localfund_top1_share`
    - `localfund_top3_share`
  - temporal behavior:
    - `localfund_streak_buy_days`
    - `localfund_netbuy_ma20`, `localfund_netbuy_std20`, `localfund_netbuy_z20`
    - `localfund_netbuy_3d_sum`, `localfund_netbuy_7d_sum`
    - `localfund_buy_freq_ma30`, `localfund_buy_freq_std30`, `localfund_buy_freq_z30`
    - `localfund_buy_freq_ratio_to_ma30`
- Training datamart expanded from ~91 columns to 120 columns.

## Data/State Findings

- Raw broksum coverage is broad, but current Level 1 snapshot is still short:
  - `broksum_bybroker.parquet`: 2024-11-01 -> 2026-04-13
  - `Level 1 broksum_datamart.parquet`: 2026-02-02 -> 2026-04-10
- Therefore current training window remains short (starts around Feb 2026) unless Level 1 is rebuilt with earlier `date-from`.
- `master_broker.parquet` integrity check:
  - all rows sum weights to 100
  - 1 category mismatch vs dominant weight:
    - broker `AO` labeled `Local fund`, dominant composition suggests `Retail`

## Current Production Artifacts

- Features Level 1:
  - `machinelearning/data/Level 1/broksum_datamart.parquet`
- Training outputs (canonical + mirrored):
  - `machinelearning/data/training/label_opening_to_1000.parquet`
  - `machinelearning/data/training/training_datamart_opening_tp3.parquet`
  - `machinelearning/data/Level 2/label_opening_to_1000.parquet` (legacy)
  - `machinelearning/data/Level 2/training_datamart_opening_tp3.parquet` (legacy)

## Next Session Priority

1. Rebuild Level 1 with longer historical window (e.g. from ~1 year back) to stabilize rolling features and reduce early-window zeros.
2. Continue localfund-first modeling path:
   - baseline time-split model
   - evaluate precision@topN for nightly screener use-case.
3. Decide category source-of-truth rule:
   - keep IDX `category` as-is, or
   - derive `category_by_weight` and use it consistently in feature grouping.
