# 2026-05-09 13:55 WIB — v23b leakage/contribution audit

- **Scope:** Training only.
- **User intent:** Focus on the v23b candidate, inspect best feature contribution, and make sure there is no small leakage/overfit issue before trusting it.

## Important Correction

The user said `v32b`, but the artifact in this branch is `v23b_ara_history_only_fullcoverage_clean`. No `v32b` model directory exists.

## Feature Contribution — Old v23b

Initial v23b family contribution before the new audit:

- pre14 intraday: ~63.8% gain
- macro prev-close: ~28.9%
- ARA-history T-1: ~2.8%
- broker aggregate: ~2.3%
- yf_daily family: ~2.1%
- CVD: ~0.2%

Read: v23b is not a pure ARA model. It is mostly a pre14/macro model, lightly helped by ARA-history.

## Residual Leak Found

Audit found `broker_aggregate_features.parquet` still carried L1 `yf_daily_*` values that matched same-day daily bars:

- `yf_daily_open_mean`: 207,949 / 208,062 comparable rows matched same-day
- `yf_daily_high_mean`: 207,949 / 208,062 matched same-day
- `yf_daily_low_mean`: 207,949 / 208,062 matched same-day
- `yf_daily_close_mean`: 207,949 / 208,062 matched same-day
- `yf_daily_range_pct_mean`: 208,062 / 208,062 matched same-day

This matters because final daily high/low/close are not available at the pre14 decision point. Therefore old `v23b_ara_history_only_fullcoverage_clean` is not clean enough to trust.

## Patch

- Patched `edges/bpjs_opening_tp3/scripts/generate_datamart.py` in `build_feature_aggregate()`.
- The shared broker aggregate builder now shifts current-row L1 numeric families per `(broker, ticker)` before date/ticker aggregation:
  - `flow_`
  - `ctx_`
  - `tfl_`
  - `yf_daily_`
  - `yf_1h_`
  - `yf_4h_`
  - `pc_`
  - `has_yf_`
- Recomputed z/velocity from shifted T-1 flow vs shifted historical baseline.

Regenerated:

- Backup: `data/Level_1_Features/modules/broker_aggregate_features.parquet.pre_yfdaily_t1_fix.bak`
- New: `data/Level_1_Features/modules/broker_aggregate_features.parquet`
- Shape: 252,926 rows x 190 cols

Post-patch exact reproduction audit:

- `yf_daily_high_mean`: 204,220 / 204,220 match shifted L1 aggregate
- `yf_daily_close_mean`: 204,220 / 204,220 match shifted L1 aggregate
- `yf_daily_range_pct_mean`: 204,220 / 204,220 match shifted L1 aggregate
- `flow_total_net_buy_mean`: 245,698 / 245,698 match shifted L1 aggregate
- `ctx_broker_market_share_mean`: 245,698 / 245,698 match shifted L1 aggregate
- `tfl_net_buy_z_20_mean`: 176,360 / 176,360 match shifted L1 aggregate

## Clean Retrain

Trained replacement candidate:

- Path: `model/BSJP/v23b_t1audit_clean/`
- Same v23b intent: ARA-history only; pre14 ARA-state remains policy/backtest-only.
- Same setup: v19d-like close10 core, feature modules, preclose14 blacklist, market execution, `pre14_market_cost_est <= 0.030`, `entry_price >= 500`, k=3, max weight 25%, p_cut=0.035, OOT 100D, md100/l1=0.5/l2=1.5/gain=0.05.

Metrics:

- Best iteration: 5
- WF AUC: 0.586 / 0.554 / 0.539 / 0.546
- OOT AUC: 0.5346
- OOT AUCPR: 0.3971
- OOT cum net: +207.9%
- OOT mean daily net: +1.22%
- OOT MaxDD: -24.2%
- Overfit gap: 0.0616

Clean v23b feature-family contribution:

- pre14 intraday: 67.1% gain
- macro prev-close: 26.7%
- ARA-history T-1: 2.6%
- yf_daily now T-1-or-older: 1.8%
- broker aggregate T-1: 1.6%
- CVD T-1: 0.07%

Top features:

- `pre14_spread_cost_est`
- `pre14_tick_pct`
- `ihsg_prev_close`
- `pre14_range_pct`
- `pre14_vwap`
- `usdidr_prev_close`
- `pre14_close_vs_open9`
- `usdidr_5d_return`
- `ihsg_close_ma20_ratio`
- `ihsg_prev_return`
- `max_return_20d_tminus1`
- `max_return_5d_tminus1`
- `days_since_last_ara`
- `last_ara_return`

Artifacts:

- `_LOG/v23b_t1audit_clean_feature_importance_20260509.csv`
- `_LOG/v23b_t1audit_clean_feature_family_20260509.csv`
- `_LOG/v23b_t1audit_clean_audit_summary_20260509.json`

## Read

- Leakage: old v23b was not clean; new `v23b_t1audit_clean` is clean against the specific residual `yf_daily` same-day leak and exact banned/outcome/policy-only checks.
- Overfit: not solved. Gap 0.0616 and best iteration 5 are still caution flags. This model is a research candidate, not production.
- The most honest next step is robustness work on `v23b_t1audit_clean`: worst-day attribution, bucket attribution, no-broker ablation, and policy stress tests.
