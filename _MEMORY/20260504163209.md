# 2026-05-04 — Go Inference: Preclose14 + v20 ARA Policy

- Date: 2026-05-04 16:32 WIB
- Scope: Implement CVD, Preclose14 features in Go. Fix yf_daily `.JK` bug. Add v20 ARA-state policy to Go predict. Calibrate.
- Product: Inference (Go binary)
- Predecessors: `20260501215236.md`

## Key Decisions

1. **Pure Go for Preclose14** — rejected Python bridge (`modules.go`), implemented full Go module with single-pass parquet read.
2. **DuckDB `INSERT OR REPLACE`** — replaced DELETE+INSERT for dedup, with flow-order fix (upsert FIRST, then broker/CVD UPDATEs).
3. **v20 policy auto-activation** — any variant containing `v19d` or `v20` in name gets `v20_clean` ARA policy. Clean switch, no extra CLI flag.

## Completed (This Session)

### Features
- **CVD**: `cvd.go` (125 lines), 6 columns via DuckDB SQL window functions. 556 rows verified.
- **Preclose14**: `preclose14.go` (450 lines), 115 columns (90+ active). Single-pass parquet read, 756 tickers.
- **Total**: 360 cols/ticker for v19d. 5 trees, 306 features.

### Calibration
- **Global**: 15/15 PERFECT
- **Momentum**: 4/4 PERFECT
- **yf_daily**: 58/58 PERFECT (after `.JK` suffix fix)
- **Preclose14 price**: 85/115 PERFECT
- **Preclose14 volume**: ~30/115 diffs (parquet-go volume read discrepancy)

### Bugs Fixed
1. yf_daily `.JK` suffix → was ALL NaN, now PERFECT
2. Duplicate key on UpsertDate → `INSERT OR REPLACE`
3. Flow order → upsert before broker/CVD UPDATEs  
4. Dedup logic → merge duplicate tickers before upsert
5. Duplicate code block in cmdFetch

### v20 ARA-State Policy
- **Mechanic**: veto after scoring, keep `single_release` + `near_ara_not_touched_0_3pct`
- **First run**: 756 → 2 picks (KOBX, NIRO). Sparse — matches research.
- **Fallback**: if all vetoed, return unfiltered picks (no silent failure)
- No impact on non-v19d/v20 variants (v15 runs clean)

## Verification

```bash
cd inferences/bsjp/golang
GOTOOLCHAIN=local go build -o bsjp ./cmd/bsjp/  # OK
./bsjp predict --variant v19d_close10_preclose14_orb_md100_l21.5 --date 2026-04-23
# → 756 picks → 2 filtered (policy=v20_clean)
# → Top: #1 KOBX (0.3676), #2 NIRO (0.3637)
./bsjp predict --variant v15 --date 2026-04-23  
# → fallback works, no filter
```

## Files Changed

- `inferences/bsjp/golang/internal/features/cvd.go` — NEW
- `inferences/bsjp/golang/internal/features/preclose14.go` — NEW (450 lines)
- `inferences/bsjp/golang/internal/features/yfdaily.go` — `.JK` fix + `strings` import
- `inferences/bsjp/golang/cmd/bsjp/main.go` — cmdFetch reorder, dedup, preclose14 wiring, v20 ARA policy inline
- `inferences/bsjp/golang/internal/db/db.go` — `INSERT OR REPLACE`
- `inferences/bsjp/golang/go.mod` — go 1.26 → 1.22
- `AGENTS.md`, `program.md`, `model/BSJP/LATEST.md` — updated

## Remaining Gaps

| Item | Est Effort | Priority |
|------|-----------|----------|
| Preclose14 volume calibration (~30 cols) | 2-4h | Medium |
| Overnight epsilon (gapdown_freq) | 1h | Low |
| Broker calibration (~187 cols) | 4-8h | Low |
| Stockbit/XL, VWAP (~10 features) | 2-4h | Low |
| Live yfinance intraday fetcher | 4-8h | Medium |
| ARA-state features in features_store (columns for policy) | done (included) | — |

## Next

1. Fix preclose14 volume diff — possibly parquet-go null handling / type casting
2. Once calibrated, wire bsjp fetch → bsjp predict to VPS cron
3. Paper-trade v20 picks with real execution for fillability assessment
