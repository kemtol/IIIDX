# Session Memory (Compact)

- Date: 2026-04-11 (Asia/Jakarta)
- Scope: Data pipeline cleanup, broker master synchronization from IDX, LLM-based broker character normalization, and production-ready ensemble merge.

## Key Decisions

1. Standardize broksum output extension to `.parquet`.
2. Daily broksum updater uses append + smart repair window (14 days).
3. Master broker must use IDX website as single source of truth.
4. `--all-brokers` fetch defaults to ACTIVE brokers only (inactive optional via flag).
5. LLM normalization consensus uses 3 models (`gpt`, `claude`, `glm`) for final production; outlier models excluded.
6. Final broker character scores are merged into `master_broker.parquet`.

## Major Changes Implemented

### Broksum / Fetching
- Updated `fetch_broksum_ipot.py`:
  - append vs overwrite update mode
  - smart repair days
  - upsert by key (`broker`, `stock_code`, `date`)
  - ACTIVE broker filtering from master broker
- Heartbeat runners created/organized under `machinelearning/service/heartbeat`.
- Cron installed for reboot + weekday schedules.

### IDX Master Broker Sync
- Added `fetch_master_broker_idx.py`:
  - Fetch directly from IDX page payload (`block.idx.id`) and parse Nuxt runtime
  - Maintain broker status: `ACTIVE`/`INACTIVE`
  - Persist sync metadata to `master_broker_idx_sync.json`
- Added heartbeat runner `run_fetch_master_broker.sh`
- Cron schedule for master sync added (reboot + weekday 17:40)

### Notebook / Data QA
- Fixed notebook extension/content corruption issue (`.ipnyb` -> `.ipynb`, escaped newline fix)
- Added starter notebook checks for broksum feature journey.

### LLM Template & Normalization
- Generated LLM template from master broker.
- Fixed malformed JSON files caused by markdown code fences.
- Compared model outputs and assessed outliers.

### Final Ensemble (Production)
- Built robust ensemble script (`ensemble_broker_character.py`) with:
  - model reliability weighting
  - confidence-adjusted robust consensus
  - integer normalization sum=100
  - final score + quality band
- Generated final `broker_character.json` and metadata.
- Merged final percentages/scores into `master_broker.parquet`.

## Current Production State

- `master_broker.parquet` now contains:
  - broker identity + status from IDX sync
  - final character percentages (`foreignfund_%`, `localfund_%`, `retail_%`)
  - ensemble diagnostics (`character_final_score`, `character_quality_band`, etc.)
- Integrity check passed: all brokers sum to 100.

## Housekeeping / Archive

- Builder JSON artifacts and ensemble script were moved to `_BAK` to keep active folders tidy.
- Active data folder now keeps operational parquet/json outputs only.

## Important Paths

- Active master: `machinelearning/data/master_broker.parquet`
- Active broksum: `machinelearning/data/broksum_bybroker.parquet`
- IDX sync meta: `machinelearning/data/master_broker_idx_sync.json`
- Heartbeat scripts: `machinelearning/service/heartbeat/`
- Archived artifacts: `machinelearning/_BAK/` and `machinelearning/service/_BAK/`

## Notes for Next Session

1. If re-running LLM ensemble, restore needed normalization JSON from `_BAK` or regenerate.
2. Keep IDX sync as authoritative broker source.
3. For low quality-band brokers, run manual review loop before strategy-level use.
