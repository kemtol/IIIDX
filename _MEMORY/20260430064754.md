# 2026-04-30 06:47 WIB — v18 fixed-universe clean test

## Scope

Training product only. Follow-up to `current_v18_fixed` investigation after user approved checking whether its strong recent performance was a useful clue.

## What changed

- Built `data/Level_2_Datamart/training_datamart_bsjp_close10_fixed_universe.parquet`.
- Source logic: take `(date,ticker)` universe from `training_datamart_bsjp_overnight_fixed.parquet`, then keep only rows that have clean close10 labels in `training_datamart_bsjp_close10_rebuild.parquet`.
- Resulting datamart: 105,793 rows, all `label_name=bsjp_close10_sl2`.
- Dropped 4,443 fixed rows because they only existed on the overnight side and had no clean close10 match.
- Trained `model/BSJP/bsjp_v18_close10_fixed_universe/`.
- Updated `model/BSJP/LATEST.md` and `program.md` with the forensic conclusion.

## Key result

Clean fixed-universe close10 failed:

| Model | 100d | 50d | 20d | MaxDD 100d | Status |
|---|---:|---:|---:|---:|---|
| `bsjp_v18_market_100d_fix` | +217.5% | +113.5% | +65.2% | -11.3% | Hybrid, not promotable |
| `bsjp_v18_close10_fixed_universe` | -41.6% | -38.8% | -15.3% | -50.2% | Clean test failed |
| `bsjp_v18_close10_rebuild_policy_w25` | +143.1% | -4.4% | +1.7% | -28.2% | Clean paper-trade candidate |

Interpretation: `current_v18_fixed` strength is not reproducible after removing mixed close10/overnight labels. Treat it as diagnostic only, not as a deployable candidate.

## Next priority

Do not promote `current_v18_fixed`. If continuing v18 work, focus on improving the clean close10 candidate's recent 50d/20d behavior, preferably by using pre-OOT validation or a genuinely forward paper-trade window rather than selecting by the same OOT.
